# 分类型 Spec 模板字段指引

在 SKILL.md 的 Spec 模板基础上，根据需求类型补充以下字段到"核心行为"部分。

---

## UI 页面 / 组件

```markdown
## 核心行为

### 用户流程
> 用户怎么到达这里，主路径是什么，离开后去哪里

### 状态覆盖
| 状态 | 触发条件 | 表现 |
|---|---|---|
| 默认 | 页面加载 | |
| 加载中 | 请求进行中 | 骨架屏 / loading |
| 空态 | 数据为空 | 空态文案 + 引导操作 |
| 错误 | 请求失败 | 错误提示 + 重试入口 |
| 无权限 | 权限不足 | 按钮置灰 或 整页拦截 |
| 禁用 | 业务条件不满足 | |

### 交互逻辑
> 只写"看代码猜不到"的部分
- 表单校验时机：
- 分页行为：
- 超长文本处理：

### 数据字段依赖
> 关键字段名和类型，方便前后端对齐
```

---

## API 接口

```markdown
## 核心行为

### 接口信息
- 方法：GET | POST | PUT | DELETE | PATCH
- 路径：`/api/v1/...`
- 认证：需要 | 不需要

### 请求参数
| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| | | | |

### 响应结构
\`\`\`json
{
  "code": 0,
  "data": {},
  "message": ""
}
\`\`\`

### 错误码
| 错误码 | 含义 | 处理建议 |
|---|---|---|
| 400 | 参数错误 | |
| 403 | 无权限 | |
| 404 | 资源不存在 | |

### 限制条件
- 频率限制：
- 数据量限制：
- 超时处理：
```

---

## 重构

```markdown
## 核心行为

### 重构范围
- 涉及文件 / 模块：
- 不动的部分：

### 重构前状态
> 现在是什么，有什么问题

### 重构后目标
> 改完应该是什么样的

### 迁移路径
> 如果有需要迁移的数据或接口，说明步骤

### 兼容性要求
- 对外接口是否保持不变：
- 是否需要灰度 / 分阶段上线：

### 验证方式
> 怎么确认重构没有引入回归
```

---

## 基础设施 / 配置变更

```markdown
## 核心行为

### 变更内容
- 变更前配置：
- 变更后配置：

### 影响范围
- 影响的服务 / 环境：
- 预期影响（性能、可用性等）：

### 回滚方案
> 如果变更有问题，怎么回滚，多快能恢复

### 上线步骤
1.
2.
3.

### 验证指标
> 上线后看哪些指标确认变更符合预期
```

---

## 数据 / Schema 变更

```markdown
## 核心行为

### 变更内容
- 新增字段：
- 修改字段：
- 删除字段：

### 迁移脚本
- 是否需要数据迁移：
- 迁移脚本路径：
- 迁移预计耗时：

### 兼容性
- 旧数据处理方式：
- 是否需要双写过渡期：

### 回滚风险
> 数据变更通常不可逆，说明回滚的可行性和风险
```
